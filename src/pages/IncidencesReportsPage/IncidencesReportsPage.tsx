import type { Props as ContainerProps } from './IncidencesReportsPage.container';
import { Styles } from './IncidencesReportsPage.styled';

interface Props extends ContainerProps {}

export const IncidencesReportsPage = (props: Props) => {
    const {} = props;
    return <section className={Styles.container}>IncidencesReportsPage</section>;
};
